services:
  - type: web
    name: insumos-y-recetas
    env: static
    staticPublishPath: dist
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 5000
