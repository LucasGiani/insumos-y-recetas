services:
  - type: web
    name: insumos-y-recetas
    env: static
    staticPublishPath: dist
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 5000
